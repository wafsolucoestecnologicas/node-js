/**
 * Exportando um objeto com a função modular hello
 */
exports.hello = (msg) => { console.log(msg); };

/**
 * Exportando um objeto com a função modular sum
 */
exports.sum = (x, y) => { console.log(x + y); };
